# Pretext 핵심 API 발표 큐시트: `prepare()` vs `layout()`

> 💡 **Core Value Proposition**  
> DOM을 단 한 번도 마운트하지 않고도 수만 개 아이템의 전체 스크롤 높이와 각 인덱스의 오프셋을 정확히 예측하여 **완벽한 60fps 스크롤 경험**을 제공합니다.

---

## 📊 한눈에 보는 비교 매트릭스

| 구분 | `prepare()` (Cold Path) | `layout()` (Hot Path) |
| :--- | :--- | :--- |
| **실행 시점** | 텍스트 변경 시 **단 1회** (Cold) | 창 리사이즈·스크롤 시 **초당 수백 번** (Hot) |
| **소요 시간** | 상대적 고비용 (수 ms) | **약 0.0002ms (0.2µs)** |
| **너비 의존성** | **Width-independent** (`maxWidth` 불필요) | **Width-dependent** (`maxWidth` 필수) |
| **DOM / Canvas** | `OffscreenCanvas` 1×1 측정 | **Zero-DOM / Zero-Canvas** (호출 0회) |
| **메모리 할당** | 고속 병렬 배열 생성 및 전역 캐싱 | **Zero-Alloc** (GC 부하 0) |
| **반환값** | `PreparedText` (불투명 핸들 / 너비 배열) | `{ lineCount, height }` (줄 수 및 높이) |

---

## [API 1] `prepare()` : 1회만 실행하는 무거운 준비 단계 (Cold Path)

> 🎤 **발표 한 줄 오프닝**  
> *"prepare()는 텍스트가 바뀔 때 딱 한 번 실행되는 사전 준비 작업입니다. 브라우저가 화면을 그리기 전에 필요한 모든 글자 메트릭을 미리 측정해서 메모리에 올려놓는 과정입니다."*

```
[Raw Text + Font] ➔ [공백 정규화] ➔ [UAX #14 분절] ➔ [OffscreenCanvas 측정 & 캐싱] ➔ PreparedText
```

### ① 너비 비의존 (Width-independent)
* 🎙️ **발표 멘트**:  
  *"가장 중요한 특징은 너비(`maxWidth`)를 몰라도 미리 측정할 수 있다는 점입니다."*
* ⚙️ **기술 디테일**:  
  컨테이너 너비가 300px이든 800px이든 상관없이 **"단어 하나하나의 고유 너비"**는 변하지 않습니다. 따라서 창 크기나 부모 박스 너비와 상관없이 텍스트가 생성되는 시점에 단 1회만 먼저 계산해 둘 수 있습니다.

### ② 공백 정규화 (White-space Normalization)
* 🎙️ **발표 멘트**:  
  *"브라우저의 CSS `white-space` 규칙대로 연속된 탭이나 띄어쓰기를 먼저 합치고 정리합니다."*
* ⚙️ **기술 디테일**:  
  HTML은 연속된 띄어쓰기나 개행(`\n`, `\t`)을 하나로 병합합니다. 줄바꿈을 계산하기 전에 브라우저의 공백 붕괴(Collapse) 규칙을 JS 레벨에서 동일하게 정규화하여 렌더링 오차를 사전에 방지합니다.

### ③ 유니코드 분절 (Unicode Segmentation)
* 🎙️ **발표 멘트**:  
  *"글자를 단순히 띄어쓰기로 자르는 게 아니라, 브라우저 표준 엔진(`Intl.Segmenter`와 UAX #14)을 써서 어디서 줄바꿈이 가능한지 쪼갭니다."*
* ⚙️ **기술 디테일**:  
  한국어/일본어 같은 CJK는 띄어쓰기가 없어도 글자 단위 줄바꿈이 일어나고, 영어는 단어 단위로 끊어집니다. 유니코드 텍스트 분절 표준(**UAX #14**)에 따라 안전하게 끊길 수 있는 최소 분절(Segment) 단위로 텍스트를 나눕니다.

### ④ 1×1 캔버스 (OffscreenCanvas 측정)
* 🎙️ **발표 멘트**:  
  *"DOM 노드를 화면에 띄우지 않고, 보이지 않는 1×1 메모리 캔버스(`OffscreenCanvas`)에서 너비만 잽니다."*
* ⚙️ **기술 디테일**:  
  화면 밖 DOM에 `<div>`를 만들어 마운트하는 대신, 순수 메모리 상의 `OffscreenCanvas` 컨텍스트에서 `ctx.measureText()`를 호출합니다. DOM 트리를 전혀 건드리지 않으므로 리플로우가 발생하지 않습니다.

### ⑤ 2계층 캐싱 (2-tier Caching)
* 🎙️ **발표 멘트**:  
  *"한 번 측정한 단어 너비는 `[폰트][단어]` 구조의 전역 해시맵에 캐싱해서 $O(1)$로 재사용합니다."*
* ⚙️ **기술 디테일**:  
  `"apple"`이라는 단어를 `16px Pretendard`로 이미 쟀다면, 다른 카드나 다음 문장에서 다시 나와도 캔버스를 호출하지 않고 메모리 캐시에서 $O(1)$로 즉시 꺼내 씁니다.

### ⑥ 엔진 결함 보정 (Platform Bug Workarounds)
* 🎙️ **발표 멘트**:  
  *"macOS 크롬이나 사파리에서 이모지가 2~4px 뚱뚱하게 측정되어 줄바꿈이 튀는 브라우저 버그까지 역으로 보정합니다."*
* ⚙️ **기술 디테일**:  
  OS/브라우저별 폰트 셰이퍼(CoreText, HarfBuzz) 간 미세한 커닝(Kerning) 차이나, macOS에서 이모지가 실제 렌더링 폭보다 캔버스에서 2~4px 넓게 측정되는 플랫폼 버그들을 엔진 차원에서 사전 보정합니다.

### ⑦ 오버플로 대비 & 불투명 핸들 반환 (Opaque Handle)
* 🎙️ **발표 멘트**:  
  *"측정한 수치들을 `PreparedText`라는 고속 병렬 배열(`Float64Array`)에 패킹해서 `layout()`으로 넘겨줍니다."*
* ⚙️ **기술 디테일**:  
  박스 너비보다 긴 단어가 들어올 때를 대비해 글리프 단위 너비(`breakableFitAdvances`)까지 미리 배열에 적재한 뒤, 개발자가 내부 구조를 임의 조작할 필요 없는 최적화된 불투명 핸들(Opaque Handle)로 반환합니다.

---

## [API 2] `layout()` : 매 프레임 무제한 반복 호출하는 극단적 핫패스 (Hot Path)

> 🎤 **발표 한 줄 오프닝**  
> *"layout()은 창 크기를 조절하거나 스크롤할 때 초당 수백 번씩 실행되는 핫패스입니다. 문자열도 캔버스도 DOM도 전혀 건드리지 않고, 오직 '숫자 덧셈과 곱셈'만 수행합니다."*

```
[PreparedText + maxWidth] ➔ [산술 누적: lineW += w] ➔ [Trailing Hanging 처리] ➔ { lineCount, height } (0.0002ms)
```

### ① 초극단 Hot Path (0.0002ms)
* 🎙️ **발표 멘트**:  
  *"실행 속도가 0.2마이크로초, 즉 0.0002ms에 불과해서 120fps 환경에서도 아무런 부담 없이 반복 호출할 수 있습니다."*
* ⚙️ **기술 디테일**:  
  브라우저 1프레임(16.6ms) 동안 수만 번을 실행할 수 있을 만큼 빠릅니다. 윈도우 리사이즈, 무한 가상 스크롤, 아코디언 애니메이션 도중에도 메인 스레드 점유율이 0에 수렴합니다.

### ② Zero-DOM (리플로우 0회)
* 🎙️ **발표 멘트**:  
  *"DOM 트리에 `offsetHeight` 같은 수치를 단 1번도 묻지 않으므로 레이아웃 스래싱이 100% 원천 차단됩니다."*
* ⚙️ **기술 디테일**:  
  기존 방식은 높이를 알기 위해 DOM을 조회하다가 C++ 레이아웃 트리를 강제로 플러시(Forced Synchronous Layout)시켰지만, `layout()`은 DOM API 호출이 아예 없어 리플로우가 0회입니다.

### ③ Zero-Canvas (캔버스 미호출)
* 🎙️ **발표 멘트**:  
  *"리사이즈할 때 `measureText()` 같은 캔버스 API조차 다시 부르지 않습니다."*
* ⚙️ **기술 디테일**:  
  캔버스 컨텍스트 호출도 상당한 오버헤드입니다. `prepare()`에서 이미 숫자 배열로 추출해 두었기 때문에, `layout()` 내부의 캔버스 호출 횟수는 정확히 0회입니다.

### ④ Zero-Alloc (가비지 컬렉션 부하 0)
* 🎙️ **발표 멘트**:  
  *"루프 안에서 새 문자열을 자르거나 객체를 생성하지 않기 때문에 가비지 컬렉터(GC)에 의한 프레임 드랍이 없습니다."*
* ⚙️ **기술 디테일**:  
  루프 내부에서 `substring()`이나 임시 객체를 생성(Allocation)하면 V8 엔진에 가비지가 쌓여 주기적인 끊김(GC Jank)이 발생합니다. `layout()`은 이미 할당된 숫자 배열의 인덱스 포인터만 전진시킵니다.

### ⑤ 순수 산술 누적 (`lineW += w`)
* 🎙️ **발표 멘트**:  
  *"동작 원리는 아주 단순합니다. 배열의 너비 숫자를 더해가면서(`lineW += w`), 컨테이너 너비를 넘치면 줄바꿈 카운트를 1 올리는 덧셈입니다."*
* ⚙️ **기술 디테일**:  
  단순 산술 누적과 `lineW <= maxWidth` 크기 비교 연산만 수행합니다. 마지막에 `줄 수 × lineHeight`를 곱해 박스 높이를 즉시 도출합니다.

### ⑥ CSS 스펙 충실 (Trailing Whitespace Hanging)
* 🎙️ **발표 멘트**:  
  *"문장 끝에 걸린 공백(Space) 때문에 줄바꿈이 튀지 않도록 브라우저의 '공백 매달기' CSS 규칙을 완벽하게 재현합니다."*
* ⚙️ **기술 디테일**:  
  CSS 명세상 줄 끝의 후행 공백(Trailing Space)은 박스 너비를 초과해도 다음 줄로 넘어가지 않고 라인 바깥에 매달려(Hanging) 있어야 합니다. 이 미묘한 CSS 표준 규칙을 수식으로 정확히 재현해 브라우저 실제 렌더링 결과와 1px 오차 없이 일치시킵니다.

### ⑦ 백트래킹 & 폴백 (Backtracking & Fallback)
* 🎙️ **발표 멘트**:  
  *"긴 단어가 박스 너비를 넘어가면, 직전 공백으로 안전하게 롤백하거나 글자 단위(Grapheme)로 쪼개는 예외 처리를 초고속으로 수행합니다."*
* ⚙️ **기술 디테일**:  
  중간에 단어가 너무 길어 넘치면 직전 공백 위치로 인덱스를 되돌리는 백트래킹(Backtracking)을 수행하고, 단어 하나가 전체 너비보다 큰 극단적 케이스에는 `prepare()`에서 사전 적재해 둔 글리프(Grapheme) 단위 배열로 즉시 전환해 안전하게 분절합니다.

---
